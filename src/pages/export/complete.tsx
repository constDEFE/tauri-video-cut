import { CompleteOutput } from "@/widgets/export/complete-output";

export const CompletePage = () => (
	<div class="bg-accent-inverted flex min-h-screen flex-col items-center justify-center p-8">
		<CompleteOutput />
	</div>
);
